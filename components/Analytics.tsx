/**
 * Analytics placeholder (optional)
 * - Google Analytics: add script tags in app/layout.tsx <head> when ready.
 * - Plausible: add the Plausible script tag.
 * Keep disabled during pre-launch unless required.
 */
export function Analytics() {
  return null;
}
